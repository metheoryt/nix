# Host: g16

<!--
Per-host memory + instructions for g16 (ASUS ROG G16, NixOS — Intel + NVIDIA).
Symlinked to ~/.claude/host-memory.md and imported by claude/CLAUDE.md, so it is
loaded ONLY when the hostname matches. Tracked in git and synced to every
machine, but inert on the others. Put machine-specific facts here: installed
tooling, local paths, hardware quirks, per-host overrides. Do NOT put secrets
here (this file is tracked in git).
-->

## Notes
